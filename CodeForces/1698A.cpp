#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, ai;
        cin >> n;
        while (n--)
            cin >> ai;
        cout << ai << endl;
    }
    return 0;
}